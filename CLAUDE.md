# CLAUDE.md — "나는 세계에서 몇 등?" 프로젝트 규칙

이 파일은 **다음 세션에서도 반드시 지켜야 하는 규칙**입니다.
작업을 시작하기 전에 이 파일과 `PROGRESS.md`를 먼저 읽으세요.

## 0. 사용자 안내 방식
- 사용자는 코딩 초보입니다. 설명은 쉬운 한국어로, 전문용어는 풀어서 씁니다.
- **한 번에 한 단계씩** 진행합니다. 단계가 끝나면 커밋하고 멈춰서,
  ① 무엇을 했는지 ② 브라우저에서 어떻게 확인하는지를 알려줍니다.
- 파일 삭제, 되돌리기 어려운 명령(`git reset --hard`, `rm -rf`, force push 등)은
  **실행 전에 이유를 설명하고 반드시 물어봅니다.**
- 매 단계마다 `PROGRESS.md`를 갱신합니다.

## 1. 프로젝트 개요
사용자가 키·몸무게·연소득을 입력하면 **세계에서 상위 몇 %인지**와
**선택한 나라에서 상위 몇 %인지**를 알려주는 다국어 사이트입니다.
수익원은 Google AdSense.

- 언어: 한국어(ko) / 영어(en) / 일본어(ja)
- 나라: 한국(KR), 미국(US), 일본(JP), 그리고 세계(WORLD)
- 대상: **만 19세 이상 성인만**
- 소득 기준: **개인 세전 연소득**으로 통일하고 화면에 명시.
  가구소득과 절대 섞지 않습니다.

## 2. 기술 스택
- Next.js(App Router) + TypeScript + Tailwind CSS
- **정적 내보내기(`output: "export"`)**. 결과물은 `out/` 폴더. **서버·데이터베이스 없음**
- 모든 계산은 **브라우저 안에서만** 수행
- 배포: **Cloudflare** (GitHub 연동). Vercel 전용 기능(`@vercel/*` 등)은 쓰지 않는다.
- 방문자 통계는 **Cloudflare Web Analytics** 를 대시보드에서 켠다. 코드로 넣지 않는다.
- 테스트: Vitest

### 정적 배포에서 지켜야 할 것
- `next.config.ts` 의 `redirects()` / `rewrites()` / 미들웨어는 동작하지 않는다.
  `/` 로 들어온 방문자는 `public/index.html` 의 스크립트가 브라우저 언어를 보고 보낸다.
- 동적 이미지 생성이 안 되므로, 공유용 OG 이미지는 `npm run build:og` 로
  빌드할 때 미리 `public/og/*.png` 를 만들어 둔다.
- `app/sitemap.ts` 와 `app/robots.ts` 에는 `export const dynamic = "force-static"` 이 필요하다.
- 404 화면은 `app/not-found.tsx` → `out/404.html` 로 만들어진다.

## 3. 개인정보 (절대 규칙)
- 사용자가 입력한 값은 **어디에도 저장하거나 전송하지 않습니다.**
- 입력값을 `fetch`, `XMLHttpRequest`, `navigator.sendBeacon`, 쿠키,
  분석 도구(analytics) 등으로 내보내는 코드를 **절대 작성하지 않습니다.**
- 결과 공유는 **URL 파라미터로만** 합니다.

## 4. 데이터 규칙 (가장 중요)
- 모든 통계 수치는 **`/data` 폴더의 JSON 파일에만** 둡니다.
  **코드(.ts/.tsx)에 통계 숫자를 하드코딩하지 않습니다.**
- 모든 수치 묶음에는 `citation` 이 붙고, 그 안에
  `source`(기관명), `url`, `year`, `license` 가 있어야 합니다.
- **정확한 수치를 확실히 알지 못하면 절대 지어내지 않습니다.**
  값은 `null`, `status`는 `"todo"`, `todo` 필드에 무엇을 확인해야 하는지 적습니다.
- `status: "todo"` 인 항목은 화면에서 **"데이터 준비 중"** 으로 표시되고,
  에러 없이 넘어가야 합니다.
- 출처 후보: NCD-RisC(세계 키), 국민건강영양조사·KOSIS(한국),
  CDC NHANES·US Census(미국), e-Stat(일본),
  World Inequality Database·World Bank(소득·PPP)
- 결과 화면 하단과 `/[locale]/sources` 페이지에 출처와 기준 연도를 표시합니다.

## 5. 계산 방식
- 키·몸무게: 정규분포(평균 `mean`, 표준편차 `sd`)로 백분위 계산
- 소득: 실제 백분위 표가 있으면 **보간**, 없으면 **로그정규분포**
- 세계 백분위: 나라별 분포를 **성인 인구로 가중한 혼합분포**로 합쳐 계산.
  "세계 평균 하나"로 퉁치지 않습니다.
- 계산 로직은 `/lib`에 **순수 함수**로 두고 **단위 테스트**를 작성합니다.
  (예: 평균값을 넣으면 약 50%가 나오는지 확인)
- 표기: `상위 12.3%` 처럼 소수 첫째 자리까지.
  극단값은 `상위 0.1% 미만` / `하위 0.1% 미만` 으로 표시합니다.

## 6. 단위와 숫자 표기
- 미국(en/US): ft·in, lb 기본 / 한국·일본: cm, kg 기본
- 사용자가 **언제든 단위를 전환**할 수 있어야 합니다.
- 한국어 소득 입력은 **"만원" 단위**를 지원합니다.
- 숫자 표기는 `Intl.NumberFormat`으로 각 언어 형식에 맞춥니다.
- 세계 소득 비교는 **PPP 기준**으로 환산하며, PPP·환율 값도
  연도와 함께 `/data/ppp.json`에 고정값으로 둡니다.

## 7. 페이지 구조
```
/[locale]                              메인 (항목 선택)
/[locale]/[metric]                     항목별 계산기
/[locale]/[metric]/[gender]/[value]    특정 수치 결과 페이지 (정적 생성)
/[locale]/about|sources|privacy|terms|contact
```
- 결과 페이지는 **현실적인 범위만** 생성합니다 (예: 키 150~200cm, 1cm 단위).
  무작정 늘리지 않습니다.
- 구글의 대량 생성 콘텐츠 정책을 고려해, 결과 페이지마다 숫자만 바뀌지 않도록
  **한·미·일·세계 비교표, 구간별 짧은 해설, 인접 수치 링크** 등
  실질적으로 다른 내용을 넣습니다.

## 8. SEO
- 페이지마다 고유한 `title`, `meta description`, 동적 OG 이미지
- `hreflang`(ko/en/ja)과 `canonical`
- `sitemap.xml`, `robots.txt`, 구조화 데이터(FAQ 등 적절한 것)
- 공유 버튼: 링크 복사, X, 카카오톡(한국어), LINE(일본어)

## 9. 광고
- 광고 자리는 `components/AdSlot.tsx` 로 **자리만** 잡고,
  실제 광고 코드는 넣지 않습니다.
- 광고 영역은 **높이를 미리 확보**해서 화면이 밀리지 않게 합니다 (CLS 방지).
- **입력창·계산 버튼·공유 버튼 바로 옆이나 위에 광고를 두지 않습니다.**
  (실수 클릭 유도 금지)
- 유럽·영국 방문자용 쿠키 동의 배너는 나중에 붙일 수 있게 구조만 준비합니다
  (`components/CookieConsent.tsx`).

## 10. 표현과 톤
- 몸무게·BMI 결과에는 **"의료·건강 진단이 아닙니다"** 안내를 넣습니다.
- 체형이나 소득을 **평가·비하·조롱하는 표현을 쓰지 않습니다.**
  중립적이고 재미있는 톤을 유지합니다.
- 모든 문구는 `/messages/ko.json`, `en.json`, `ja.json` 에 분리합니다.
  **코드에 한글·영문·일본어 문장을 직접 쓰지 않습니다.**
  (코드 주석은 한국어로 써도 됩니다.)
- 번역은 각 언어 원어민이 쓰는 자연스러운 표현으로 합니다.
  (기계 번역투 금지)

## 11. 디자인
- **모바일 우선** (375px에서 정상 동작 필수), 깔끔하고 신뢰감 있는 디자인
- 결과는 분포 곡선 위에 "나의 위치"를 표시하고 "상위 X%"를 크게 보여줍니다.
- 다크모드 지원 (`<html class="dark">` 방식)
- 빠른 로딩 (Lighthouse 성능 90점 이상 목표)

## 12. 단계 완료 체크리스트
각 단계를 마칠 때 아래를 확인하고 결과를 보고합니다.
- [ ] `npm run build` 가 에러 없이 성공
- [ ] `npm test` (계산 함수 단위 테스트) 통과
- [ ] `/data`의 모든 수치에 `source`, `url`, `year` 가 있고,
      확인 안 된 값은 `null` + `status: "todo"`
- [ ] `null` 데이터가 있어도 페이지가 깨지지 않음
- [ ] ko/en/ja 세 언어와 단위 전환이 정상 동작
- [ ] 모바일 화면(375px)에서 레이아웃 정상
- [ ] 입력값이 네트워크로 전송되지 않음
- [ ] 광고 자리가 입력·버튼 근처에 없고 화면 밀림이 없음
- [ ] `sitemap.xml`에 결과 페이지가 포함되고 `hreflang`이 올바름

## 13. 자주 쓰는 명령어
```bash
npm run dev        # 개발 서버 (http://localhost:3000)
npm run build      # 실제 배포용 빌드 (에러 확인)
npm test           # 계산 함수 테스트
npm run lint       # 코드 스타일 검사
npm run typecheck  # 타입 검사
```

## 14. Git
- 작업 브랜치: `claude/gracious-davinci-25ef65`
- 다른 브랜치에는 **허락 없이 푸시하지 않습니다.**
- 커밋 메시지는 무엇을 왜 했는지 알 수 있게 씁니다.
